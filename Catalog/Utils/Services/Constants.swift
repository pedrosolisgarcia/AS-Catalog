public enum API: String {
  case SCHEME = "https"
  case HOST = "host.server.com" // Not prod Host. Please request
  case PATH_CUSTOMER = "/production/customers"
  case PATH_COLLECTION = "/production/v1/collections/latest"
  case PATH_SHOPID = "/production/v1/shops/availability"
}
